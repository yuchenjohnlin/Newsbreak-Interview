# One-Sentence Topic Page Generator

Type one sentence describing a hot event → get a publishable HTML **topic page**,
built from fresh, cited web sources. Open the output file directly in a browser.

## What this is
TODO — short product description.

## Prerequisites
- An LLM API key and a web-search / research API key.
  Copy `.env.example` → `.env` and fill them in.
- TODO — runtime (Python / Node / …).

## Install
TODO

## Usage
TODO — e.g.

```
<run command> "OpenAI rolled out GPT-5.5 Instant as the default model in ChatGPT in May 2026."
```

Output is written to `out/<id>.html` — open it directly in a browser.

## Example outputs
Three built pages live in `out/` (committed):
- TODO
- TODO
- TODO

## How it works
See [DESIGN.md](DESIGN.md) and [docs/architecture.md](docs/architecture.md).
