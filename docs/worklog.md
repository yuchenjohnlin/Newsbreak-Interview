# Worklog

One line per work session. Newest at top.
Format: `YYYY-MM-DD — what changed / what's next`.

- 2026-06-08 — Python project skeleton: pyproject, config/io helpers, 6 stage stubs, CLI orchestrator. Decided LLM emits data not HTML (ADR 0005); no frontend/backend. Next: grab a search API key (Tavily) → build the retrieval spike in `retrieve.run`.
- 2026-06-08 — Scaffolded repo structure, docs, and ADR skeletons.
